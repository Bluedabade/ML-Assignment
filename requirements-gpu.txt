# Install this file inside Ubuntu/WSL2 for NVIDIA GPU training.
# TensorFlow's CUDA runtime libraries are supplied by the pip extra; do not
# install another tensorflow package into the same virtual environment.
tensorflow[and-cuda]==2.16.1
keras==3.15.1
numpy==1.26.4
pandas==2.2.2
matplotlib==3.9.0
scikit-learn==1.5.0
seaborn==0.13.2
Pillow==10.3.0
tqdm==4.66.4
jupyter==1.0.0
